{
  "name": "prime-topup",
  "version": "1.0.0",
  "description": "PRIME TOPUP - Landing page jasa top up game",
  "scripts": {
    "build:css": "npx tailwindcss -i ./input.css -o ./styles.css --minify",
    "watch:css": "npx tailwindcss -i ./input.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
